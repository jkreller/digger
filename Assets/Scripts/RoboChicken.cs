using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RoboChicken : RoboEnemy
{
    // Start is called before the first frame update
    void Start()
    {
        base.Start();
    }

    // Fixed Update is called once per frame
    void FixedUpdate()
    {
        base.FixedUpdate();
    }
}
